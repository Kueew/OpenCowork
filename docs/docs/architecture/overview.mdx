---
title: 整体架构概述
description: OpenCowork 的整体架构设计与模块关系。
---

# 整体架构概述 / Architecture Overview

OpenCowork 当前是一个以 Electron 为宿主、React 为交互层、SQLite 为本地状态底座、主进程 Node.js Agent Runtime 为执行中枢的新一代桌面 Agent 工作台。主进程负责系统能力、进程编排、Provider I/O 与持久化；渲染进程负责 UI、审批与状态展示；预加载脚本只暴露最小桥接面；复杂任务可下沉到主进程 runtime 或独立 worker。

## 架构图 / Architecture Diagram

```mermaid
flowchart TD
    subgraph Renderer["渲染进程 (React 19 + Zustand)"]
        UI[App / Notify UI]
        AS[Agent Store]
        CS[Chat Store]
        TR[Tool Registry]
        TEAMS[Team Runtime / Sub Agents]
        MCPR[MCP / Channel / App Plugin adapters]
    end

    subgraph Preload["预加载脚本"]
        Bridge[window.electron + window.api]
    end

    subgraph Main["主进程 (Electron + Node.js)"]
        WIN[Window / Tray / Crash / Updater]
        IPC[IPC Handlers]
        DB[(SQLite)]
        CH[ChannelManager]
        MCPM[McpManager]
        CRON[Cron Scheduler]
        AR[Agent Runtime]
        SSH[SSH + Process Runtime]
    end

    UI --> AS
    UI --> CS
    AS --> TR
    AS --> TEAMS
    AS -->|HTTP/Stream| LLM[LLM APIs]
    TR --> MCPR
    MCPR -->|IPC| Bridge
    Bridge --> IPC
    IPC --> DB
    IPC --> CH
    IPC --> MCPM
    IPC --> CRON
    IPC --> AR
    IPC --> SSH
    CH --> Platforms[Feishu / Telegram / Discord / WhatsApp / WeCom / QQ / Weixin]
```

## 分层职责 / Layer Responsibilities

### 主进程 (`src/main/`)

| 模块 | 职责 |
| --- | --- |
| `index.ts` | 应用启动、窗口/托盘、IPC 注册、代理与缓存配置、托管服务初始化 |
| `ipc/` | 文件、Shell、数据库、Git、通知、截图、OAuth、MCP、Cron、Agent Runtime 等领域处理器 |
| `channels/` | 多消息平台接入、解析器注册、自动回复链路、插件化平台适配 |
| `cron/` | 定时任务调度、恢复持久化任务、执行记录 |
| `db/` | SQLite 建表、迁移、DAO |
| `mcp/` | MCP Server 生命周期与工具接入 |
| `ssh/` | SSH 会话与远程执行能力 |
| `image/` | 图像下载、剪贴板、GIF 等系统能力 |
| `window-ipc.ts` | 向窗口安全推送主进程事件 |
| `updater.ts` / `crash-logger.ts` | 自动更新与崩溃诊断 |

### 预加载脚本 (`src/preload/`)

| 模块 | 职责 |
| --- | --- |
| `index.ts` | 暴露标准 Electron API 与自定义 `window.api` |
| `window.api.*` | 图像能力、Team Runtime、独立 Team Worker 等最小必要桥接 |

### 渲染进程 (`src/renderer/src/`)

| 模块 | 职责 |
| --- | --- |
| `App.tsx` / `main.tsx` | 主窗口与通知窗口渲染入口 |
| `lib/agent/` | 会话编排、流式消息消费、上下文压缩、审批、子代理、团队协作 |
| `lib/tools/` + `lib/agent/tool-registry.ts` | 工具定义、注册、执行与审批策略 |
| `lib/mcp/` / `lib/channel/` / `lib/app-plugin/` | 外部能力与桌面工具接入层 |
| `stores/` | chat、agent、provider、settings、task、plan、team、cron、mcp、ssh、resources 等状态域 |
| `components/` / `hooks/` | UI 组件与交互逻辑 |

### 主进程 Agent Runtime (`src/main/ipc/js-agent-runtime.ts`)

| 模块 | 职责 |
| --- | --- |
| `js-agent-runtime.ts` | IPC runtime 入口、请求分发、事件转发 |
| `cron-agent-background.ts` | 统一 Agent Loop、Provider 重试、工具执行链路 |
| `api-proxy.ts` | HTTP 代理、状态码与异常重试、circuit 控制 |
| `responses-websocket-session-manager.ts` | Responses WebSocket 连接、恢复与 fallback |

## 核心设计要点 / Key Design Choices

- **本地优先**：会话、任务、计划、项目、定时任务、Wiki、SSH 连接等都落 SQLite。
- **进程边界清晰**：系统访问、Provider 请求与重试集中在主进程，UI 与会话展示保留在渲染进程。
- **统一执行运行时**：Agent 请求、工具调度、WebSocket/HTTP 重试都收口到主进程 JS runtime，避免多实现分叉。
- **项目化工作空间**：数据模型已从单纯 session 扩展到 `project -> session`，支持本地目录、SSH、插件来源。
- **可插拔扩展**：消息平台、MCP、技能、命令、提示词、应用插件都通过注册式架构接入。

## 关键数据流 / Data Flow

1. 你在 UI 中发起消息或操作。
2. `chat-store` / `agent-store` 更新前端运行时状态。
3. Agent 直接请求模型 API，流式接收文本、思考与工具事件。
4. 工具执行按能力路由到主进程工具、MCP、主进程 IPC、Team Runtime 或受控的渲染层桥接。
5. 主进程负责数据库写入、系统调用、定时任务、平台消息、SSH 与 Agent Runtime 编排。
6. 执行结果回流到 store，再驱动主窗口、通知窗口或外部消息平台输出。
